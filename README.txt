En este commit en la rama main, se añade este archivo, y se modifica el archivo .gitignore para que Git ignore
la carpeta de configuración de Intellij .idea

Esta línea se introduce desde la rama feature/tarea1

Esta línea se introduce desde la rama feature/tarea2